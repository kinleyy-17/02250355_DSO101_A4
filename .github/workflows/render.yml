name: Deploy to Render

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Verify files
      run: echo "Code pushed successfully! Ready to deploy."

    - name: List files
      run: ls -la
